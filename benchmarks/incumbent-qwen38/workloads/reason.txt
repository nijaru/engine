I need a genuinely novel design (not a standard pattern) for a rate limiter that must (a) never let a client exceed 100 requests per rolling hour even across 5 stateless replicas with no shared store, (b) degrade gracefully under clock skew up to 30 seconds between replicas, and (c) fail open only for requests smaller than 1KB. Walk through the tradeoffs of your design, explain which guarantee weakens first under load, and argue why an alternative design you rejected is worse. Be precise about where state lives and what happens during a network partition between replicas.

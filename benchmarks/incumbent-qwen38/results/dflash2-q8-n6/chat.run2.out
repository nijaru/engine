We need answer user: "Write a Python function that takes a list of (name, price, quantity) tuples and returns the total inventory value, rounding to 2 decimals. Then explain how you would handle a SKU with a negative quantity, and what invariants you would enforce at the input boundary. Include two pytest cases."

Need produce final with code, explanation, pytest cases. Need think details.

Function: total_inventory_value(items: list[tuple[str, Decimal? float? int?]]) -> float? Rounding to 2 decimals. Need decide types. For money, use Decimal to avoid float issues. But user says list of (name, price, quantity) tuples. Could accept price as Decimal or float? Better robust: convert price to Decimal(str(price)) to avoid binary float artifacts. Quantity int. Return float? "rounding to 2 decimals" could return float rounded. But for money, return Decimal quantized to 0.01. But if tests expect float? We can return float(total.quantize(Decimal('0.01'), rounding=ROUND_HALF_UP)). Or return Decimal. The prompt says returns total inventory value, rounding to 2 decimals. In Python, round(x,2) returns float. But for inventory value, Decimal is better. Could provide function returning float for simplicity, but mention Decimal. Need include two pytest cases. Could use pytest.approx? If return float, test exact? Use Decimal? Let's design:

from decimal import Decimal, ROUND_HALF_UP

def total_inventory_value(items):
    total = Decimal('0')
    for name, price, quantity in items:
        # validate? maybe boundary invariants separate.
        if not isinstance(quantity, int) or isinstance(quantity, bool): raise TypeError
        if quantity < 0: raise ValueError("quantity must be non-negative")
        p

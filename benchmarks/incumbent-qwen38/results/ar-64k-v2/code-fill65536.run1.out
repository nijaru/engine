We need answer user's request. Need produce final with complete module pipeline_aggregator as single code block. Need analyze requirements.

User provided 252 modules pipeline_stage_0..251, each identical except name and run_stage_N. They ask: Based on pipeline modules above, write a new module `pipeline_aggregator` that:
1. Collects results from run_stage_0 through run_stage_251 with the retry semantics shown,
2. Aggregates per-stage record counts into a summary dict,
3. Stops at the first non-retryable StageError and returns partial results plus the error.
Emit complete module as a single code block.

Need infer retry semantics shown. In each stage: StageError has retryable bool. run_stage_N catches KeyError and raises StageError(... retryable=False). There is MAX_RETRIES=3, DEFAULT_TIMEOUT_MS, but no actual retry logic in run_stage_N. "with the retry semantics shown" likely means aggregator should retry retryable StageErrors up to max_retries? But shown only StageError.retryable and MAX_RETRIES constant. Need implement aggregator that calls each stage, if StageError retryable, retry up to config max_retries? Need decide.

Need likely import all stage modules? Since modules are given as separate code blocks but in real environment maybe available as pipeline_stage_0 etc. We need write module that collects results from run_stage_0 through run_stage_251. Could use importlib to dynamically import pipeline_stage_{i} and getattr run_stage_{i}. Or explicit imports? Complete module single code block. Dynamic import is concise and robust. Need maybe avoid importing 252 modules if not available? But based on modules above, they exist. Could also define STAGE_COUNT=252.

Need aggregate per-stage record counts into summary dict

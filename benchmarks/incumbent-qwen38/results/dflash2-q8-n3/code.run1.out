We need answer user's request. Need produce final with full updated code single code block then short bullet list behavior changes. Need think through implementation.

We need modify retry decorator:
- retries cancelled promptly when a threading.Event is set
- delay respects caller-provided deadline (monotonic)
- logs each retry at DEBUG with attempt number, delay, and exception repr
- emit full updated code as single code block, then short bullet list.

Need decide API. Current decorator signature retry(max_attempts=3, base_delay=0.1, jitter=0.5, exceptions=(Exception,)). Need add parameters? Caller-provided deadline monotonic and threading.Event. Could add optional cancel_event=None, deadline=None? But "caller-provided deadline" likely decorator accepts deadline? Or wrapper can accept? Need design.

Task: modify it so that (1) retries are cancelled promptly when a threading.Event is set, (2) the delay respects a caller-provided deadline (monotonic), and (3) logs each retry at DEBUG... We can add parameters to retry: cancel_event: threading.Event | None = None, deadline: float | None = None. But "caller-provided deadline" could mean the decorated function caller can pass deadline? Hmm.

In Python repo, retry decorator. To allow caller-provided deadline, maybe add optional keyword argument to decorated function? But that changes function signature. Better: decorator factory accepts deadline and cancel_event. But "caller-provided" could mean the caller of retry() provides deadline. The phrase: "the delay respects a caller-provided deadline (monotonic)" likely means retry decorator can be configured with a deadline (time.monotonic() value) and sleep won't exceed it. Could also support context? Need robust.

Need prompt cancellation when threading.Event set. If using time.sleep, not prompt. Use event

name: Preallocate CUDA Argmax Scratch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/preallocate-argmax.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.98.0
          components: rustfmt, clippy
      - name: Preallocate greedy selection result storage
        run: python .github/preallocate_argmax.py
      - name: Format
        run: cargo fmt --all
      - name: Test
        run: cargo test --workspace
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: NVIDIA CUDA feature clippy
        run: cargo clippy -p engine-nvidia --features cuda --all-targets -- -D warnings
      - name: Server CUDA feature clippy
        run: cargo clippy -p engine-server --features cuda --all-targets -- -D warnings
      - name: Commit changes and remove one-shot files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/preallocate_argmax.py .github/workflows/preallocate-argmax.yml
          git add crates/nvidia
          git commit -m "Reuse CUDA greedy selection output storage"
          git push origin HEAD:main
